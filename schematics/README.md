# Schematics

The Day 1–3 electrical results come from authored SPICE templates under
`netlists/` and deterministically rendered decks under `results/generated/`.
The final annotated Xschem transistor-level schematic is still pending; no
generated deck should be hand-edited or presented as that source schematic.

Planned source schematics remain:

- `device_characterization.sch`
- `current_mirror.sch`
- `bias_network.sch`
- `two_stage_ota.sch`
- verification testbenches under `testbenches/`
