# 旧两级 OTA：标称条件复现通过

报告：`project1_basic_report_20260913T015701Z_582c6ec8.zip`  
报告 SHA256：`1145038acb946f06e97a5995308f9316bdc49217f6674bbf2f41e11436ec9eb4`  
运行包：1.0.4p3；包清单 SHA256：`a77492fe2de2aee32ab9021c867bfb1b38a66ed6e5826b4c9e3a51211585336e`

本地已核对原始报告、源码清单、原生导出网表、实际执行输入、器件工作点、完整 CSV、仿真日志和冻结旧结果。四个标称任务均通过原有 Day 4 标准。本次复核没有启动本地 Cadence，也没有改变上传包、电路或判定门槛。

## 本次实际测了什么

条件为 TT、1.8 V、27°C，输出负载 5 pF 并联 100 kΩ。工作点、AC 与环路测试的输入共模为 0.9 V；阶跃输入为 0.8→1.2→0.8 V，边沿 20 ns。

| 测试 | 验证目的 | Cadence 结果 | 冻结 ngspice 结果 | 旧门槛 |
|---|---|---:|---:|---:|
| 工作点 | 偏置与静态供电功耗 | 271.8276 µW | 270.5370 µW | ≤600 µW |
| AC | 低频差分增益 | 67.6836 dB | 67.6760 dB | ≥50 dB |
| 环路 | 单位增益频率 | 16.7329 MHz | 16.7454 MHz | ≥5 MHz |
| 环路 | 相位裕度 | 68.7881° | 69.0829° | ≥55° |
| 阶跃 | 上升转换速率 | 8.2935 V/µs | 8.2020 V/µs | ≥2 V/µs |
| 阶跃 | 下降转换速率 | 11.4567 V/µs | 11.5199 V/µs | ≥2 V/µs |
| 阶跃 | 最差 1% 建立时间 | 74.1862 ns | 74.7500 ns | ≤1500 ns |

标称输出静态电压为 0.900012 V。13 个 MOS 均导出有限的 ids、gm、gds、vds、vdsat，按已有饱和判据均通过；0.9 V 共模下最小饱和裕量为 M5 的 116.19 mV。阶跃测试初始输入为 0.8 V，对应 M5 初始裕量只有 35.67 mV；这并非整个瞬态过程中所有器件都保持饱和的证明。

两组频域记录各含 1081 个点，完整覆盖 1 Hz～1 GHz；环路只有一个向下的 0 dB 交越，低频符号正确。阶跃记录包含 10116 个点，覆盖 0～5 µs，时间间隔不超过 0.5 ns。全部指标已由 CSV 重新计算，并与学校生成的 metrics.json 核对。

上升沿上冲为 **74.096 mV**，约为 0.4 V 阶跃的 **18.52%**；冻结旧波形的上冲为 **74.997 mV**。它是原设计已有的响应特征，此次保留记录。旧标准未单独限制上冲。“1% 建立”是进入并保持在目标值 ±4 mV 内，不能当作新版 ADC 的 0.25 LSB 建立验收。

![实际波形与旧基准对照](nominal_comparison.png)

## 上次修复是否有效

有效。`P01_op RESUMED_EXPORT` 使用上次已经收敛的原始 PSF，成功读取封装子电路内部的 13 个实际 MOS，补齐工作点导出并重新评估。已核对恢复记录和原始 PSF 哈希，旧失败记录完整保留。

`P01_op ALREADY_ATTEMPTED` 表示后续标称批处理跳过刚恢复成功的工作点任务，属于正常续跑。AC、环路和阶跃是本次在学校新执行的 Spectre 仿真。

本次补丁只修正读取结果和错误处理；没有调整 MOS 尺寸、偏置或补偿参数。此前学校 CDF 的宽度取整和 M7 分拆仍保留在冻结尺寸映射中。与 ngspice 的小差异记录为迁移差异，尚未逐项分离模型、尺寸映射与求解器的贡献。

## 日志与适用范围

四项 Spectre 均正常结束，分别用 11、16、16、13 次迭代完成 DC 求解，均为 0 errors、0 warnings；另有 3、4、4、3 条 notices。保留了 `Bad pivoting` 数值求解提示，本次未为消除提示而更改求解选项。

OCEAN 启动仍记录旧主机日志锁、库重复定义、字体和调试器提示；本次导出完整且未出现器件 OP 读取错误。OCEAN 没有单独保存数值退出码文件，本地结论依据返回状态、导出完成标志、日志及数据完整性，不虚构额外退出码证据。

结论限于旧 OTA 标称复现。工艺电阻、MIM 和 RC 已通过默认尺寸下的 TT/27°C 复核；旧 OTA 的 2 kΩ 补偿电阻与 3 pF 电容仍按冻结基准使用理想元件，不能把独立工艺无源测试通过理解为已经替换了 OTA 内的补偿元件。PVT、额外负载/共模/噪声、新版前端、ADC 和版图后仿真仍是后续项目。

## 下一步：直接运行现有 PVT 组

**执行位置：学校 Linux 终端。此次不需要上传文件或在 CIW 中重新 load。**

复制整行执行：

```bash
bash ~/cadence_skywater/p1_school_run_v1.sh run --group pvt
```

该命令串行运行 P02～P13 的工作点、AC、环路、阶跃，共 48 项。加上已完成的 P01，即原计划的 13 点核心 PVT，不是新版前端要求的 45 条件全组合。

| 条件 | 工艺角 | 电源 | 温度 |
|---|---|---|---|
| P01（已完成） | TT | 1.8 V | 27°C |
| P02～P05 | FF / SS / FS / SF | 1.8 V | 27°C |
| P06～P08 | TT | 1.62 V | −20 / 27 / 85°C |
| P09～P10 | TT | 1.8 V | −20 / 85°C |
| P11～P13 | TT | 1.98 V | −20 / 27 / 85°C |

它测试同一份电路在制造速度、电源和温度变化下是否仍满足旧要求，不是在修改电路。PVT 中的 `PASS FAIL` 会被保留并继续覆盖其余条件；工具、导出或数据错误会停止运行。此次不加 `--retry`，避免重跑已有尝试。

等命令结束，或因错误返回终端提示符后，执行：

```bash
bash ~/cadence_skywater/p1_school_run_v1.sh collect
```

下载最后打印路径对应的新报告 ZIP，发回本任务。若中途停止，不要盲目 retry；先保留并回传报告。随后对照旧 PVT 的每一个通过/失败点，再安排已有额外测试组。

## English technical summary

The mapped legacy two-stage OTA passes the frozen Day 4 nominal acceptance criteria at TT, 1.8 V and 27°C with a 5 pF || 100 kΩ load. Returned native netlists, executed decks, device operating points, complete CSV records and Spectre logs were reviewed. Measured gain is 67.6836 dB, loop unity frequency 16.7329 MHz, phase margin 68.7881°, static supply power 271.8276 µW, and worst 1% settling time 74.1862 ns. All 13 MOS operating-point records are complete. Rising-step overshoot is 74.096 mV versus 74.997 mV in the frozen ngspice waveform. The OP export was recovered from retained PSF without another Spectre run; AC, loop and transient were newly executed at the school. All four Spectre results report zero errors and warnings, with retained bad-pivot notices. Qualification does not extend to PVT, the newer frontend/ADC, process-passive replacement inside the OTA, or physical implementation.
